type Game = {
  header: string;
  selector: string;
  syntax: string;
  htmlCode: string;
  helpTitle: string;
  helpSubTitle: string;
  description: string;
  examples?: string[];
}

export default Game;